target-version = "py313"
line-length = 100

[lint]
select = ["E", "F", "I", "B", "C4", "SIM", "UP"]

[lint.per-file-ignores]
# Migrations are generated by `manage.py makemigrations`; their formatting is Django's, not ours.
"**/migrations/*.py" = ["E501", "I001"]
